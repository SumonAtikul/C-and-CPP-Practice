#include<iostream>
#include<string>
#include<algorithm>
#include<cstdio>
using namespace std;
int main()
{
    string st, st2="";
    cin >> st;
    for(int i=0; i<st.size(); i++){
        st[i] = toupper(st[0]);
        st[i] = tolower(st[1<st.size()]);

        }

        cout << st << endl;
    }
